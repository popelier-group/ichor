# The packaging version, read by setuptools through `version = attr:` in setup.cfg.
# The three ichor packages are released together, so this tracks ichor.core.
# The published name of this iteration of ICHOR is ichor.core.ICHOR_RELEASE.
__version__ = "5.0.0"
